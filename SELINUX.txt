Content-Type: text/x-zim-wiki
Wiki-Format: zim 0.4
Creation-Date: 2018-02-15T11:00:19+10:00


# getenforce
# setenforce 1|0
**FCONTEXT**
# semanage fcontext -l 
# semanage fcontext -a -t httpd_content_t '/folder(/.*)?'
# semanage fcontext -d "/folder(/.*)?"
# restorecon -rv [[/dir/file]]
**PORT**
# semanage port -l
# semanage port -a -t http_port -p tcp 84
**BOOLEANS**
# getsebool -a
# semanage booleans -l
# setsebool -p http_enable_homedirs on|off
**SEALERT**
# yum install setroubleshoot-server
# sealert -a /var/log/audit/audit.log

**SELINUX DENIALS**
# ausearch -m avc -m user_avc -m selinux_err -m user_selinux_err -i -ts today

**VERIFY ALLOWED ACCESS ON SERVICES**
# sesearch --allow -s sshd_t -t spc_t
