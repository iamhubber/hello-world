Content-Type: text/x-zim-wiki
Wiki-Format: zim 0.4
Creation-Date: 2018-02-20T10:07:00+10:00

**FLOW**
https://access.redhat.com/solutions/415513

**PAM STACK**
REQUISITE
 + success = return immediately 
 + fail = returns immediately 
REQUIRED
 + success = 
 + fail = continue to the end of stack but returns a fail regardless of succeding stack results
SUFFICIENT
 + success = return immediately only if all prior modules in the stack returned success
 + fail = continue but returns success/fail depending on the succeding stack results



**AD INTEGRATION**
# yum install nscd pam_ldap nss-pam-ldap pam_krb5 krb5_workstation
CONFIG FILES
# /etc/nsswitch.conf
# [[/etc/krb5.conf]]
# [[/etc/pam.d/system-auth]]
# [[/etc/nslcd.conf]]
	uri, base, binddn,bindpw,mapping for AD(uncomment all lines)
AUTHCONFIG (use authconfig-tui if possible to update the config files above)
# authconfig --update --enablekrb5 ???

**PAM_TALLY**
https://access.redhat.com/solutions/37687

**ACCESS.CONF**
https://access.redhat.com/solutions/70472

**PASSWORD POLICY**
RHEL7
pam_pwquality module
RHEL6
pam_cracklib.so module 

**DOCU**
# grep -i sufficient /usr/share/doc/pam*/*
